#include <bits/stdc++.h>
using namespace std;

int f(vector<int> &x, int n, int m) {
    int ans=0, sub=0, cap=0;

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m; cin>>n>>m;
    vector<int> x(n);
    for (int i=0; i<n; i++) cin>>n[i];

    return 0;
}

