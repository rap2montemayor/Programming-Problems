#include <bits/stdc++.h>
using namespace std;

int main() {
    int n,m,k; cin>>n>>m>>k;
    vector<int> a;
    for (int i=0; i<n; i++)
        cin>>a[i];
    sort(a.begin(), a.end(), greater<int>());

    for (int i=0; i<
    return 0;
}
